/*#include <iostream>
#include <vector>

int main() {
    std::vector<int> intlist{27};
    intlist.push_back(2);
    std::cout << intlist[0];
    
    return 0;

}
*/

#include <iostream>
#include <vector>

int main() {
    std::vector<int> intlist{27,39};
    intlist.push_back(2);
    std::cout << intlist[0] << "\n";  // Correct access using subscript operator
    std::vector<int> vec{1,3};
    vec.swap(intlist);
    std::cout << intlist[1];
    int cap = vec.size();
    //std::cout << cap; 
    return 0;
}